{% extends 'base.html.twig' %}

{% block title %}Hello ReactController!{% endblock %}

{% block body %}
    <div class="bg-white">
        <div class="mx-auto max-w-7xl py-16 px-4 sm:py-24 sm:px-6 lg:px-8">
            <div class="text-center">
                <p class="mt-1 text-4xl font-bold tracking-tight text-gray-900">Testeur fou</p>
                <p class="mx-auto mt-5 max-w-xl text-xl text-gray-500">Ceci est un testeur qui ne s'arrete pas</p>
                <div {{ react_component('Hello', { 'fullName': 'dada' }) }}></div>
                <div {{ react_component('Modal', { 
                    'buttonOpen': 'Ouvrir',
                    'buttonClose': 'Fermer'
                }) }}></div>
            </div>
        </div>
    </div>
{% endblock %}
